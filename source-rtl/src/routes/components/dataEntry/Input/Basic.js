import React from "react";
import { Card, Input } from "antd";

const Basic = () => {
  return (
    <Card className="gx-card" title="پایه">
      <Input placeholder="استفاده پایه" />
    </Card>
  );
};

export default Basic;
